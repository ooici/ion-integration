# Override configuration file for "ion.config". Any entry in here replaces
# or adds configuration in ion.config.
#
# For a local configuration, copy to this file to "ionlocal.config" and modify settings here.
#
{

# To set the broker to local host:
'ion.test.iontest':{
    'broker_host' : 'localhost',
    'broker_port': 5672,
    'broker_vhost': '/'
    },


}